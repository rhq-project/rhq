<plugin name="Hosts"
        displayName="Hosts"
        description="Support for hosts file"
        package="org.rhq.plugins.hosts"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="urn:xmlns:rhq-plugin"
        xmlns:c="urn:xmlns:rhq-configuration">

   <service name="Hosts File"
            description="the local file used for mapping hostnames to IP addresses"
            discovery="HostsDiscoveryComponent"
            class="HostsComponent"
            supportsManualAdd="true">

      <plugin-configuration>
          <c:simple-property name="path" displayName="Hosts File Path" type="file" required="true"
                             description="the absolute path of the hosts file (e.g. /etc/hosts on Unix systems or C:\windows\system32\drivers\etc\hosts)"/>
      </plugin-configuration>

      <resource-configuration>
         <c:list-property name="entries">
            <c:map-property name="entry">
               <c:simple-property name="ipAddress" displayName="IP Address" type="string" required="true" activationPolicy="immediate"
                                  description="The IP address of the host entry. The address may be an IPv4 address (e.g., 192.168.1.10) or an IPv6 address (e.g., fec0:0:0:bebe::2)."/>
               <c:simple-property name="canonicalName" displayName="Canonical Hostname" type="string" required="true" activationPolicy="immediate"
                                  description="The canonical hostname to refer to the IP address. Host names may contain only alphanumeric characters, minus signs ('-'), and periods ('.'). They must begin with an alphabetic character and end with an alphanumeric character. (e.g., foo.mydomain.org)"/>
               <c:simple-property name="aliases" displayName="Aliases" type="longString" required="false" activationPolicy="immediate"
                                  description="Optional aliases provide for name changes, alternate spellings, shorter hostnames, or generic hostnames (e.g., localhost); aliases should be newline or space separated)."/>
            </c:map-property>
         </c:list-property>
      </resource-configuration>

   </service>

</plugin>