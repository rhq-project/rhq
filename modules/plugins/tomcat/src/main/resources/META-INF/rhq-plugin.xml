<?xml version="1.0" encoding="UTF-8" ?>

<plugin name="Tomcat"
        displayName="Tomcat Web Application Container"
        package="org.rhq.plugins.tomcat"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="urn:xmlns:rhq-plugin"
        xmlns:c="urn:xmlns:rhq-configuration">

   <depends plugin="JMX" />
   
   <server name="Tomcat Server"
           description="Tomcat Web Application Container"
           subCategory="Web Application Container"
           discovery="TomcatDiscoveryComponent"
           class="TomcatServerComponent">

      <plugin-configuration>
         <c:group name="connection" displayName="Connection Info">
            <c:simple-property name="installationPath" displayName="Installation Path" description="Directory in which the Tomcat instance is installed."/>
            <c:simple-property name="jmxUrl" displayName="JMX URL" description="URL for JMX communication. Protocol, host, port are required."/>
         </c:group>
         <c:group name="control" displayName="Controls System">
            <c:simple-property name="startScript" displayName="Start Script" type="file"
                               description="The path to the script or executable used to start this instance (e.g. &lt;install-home&gt;/bin/startup.sh); if the path is not absolute, then it will be resolved relative to the installation directory (i.e. the 'Installation Path' connection property)."/>
            <c:simple-property name="shutdownScript" displayName="Shutdown Script" type="file"
                               description="The path to the script or executable used to stop this instance  (e.g. &lt;install-home&gt;/bin/shutdown.sh); if the path is not absolute, then it will be resolved relative to the installation directory (i.e. the 'Installation Path' connection property)."/>
         </c:group>
      </plugin-configuration>

      <process-scan name="Tomcat" query="process|basename|match=^java.*,arg|org.apache.catalina.startup.Bootstrap|match=.*"/>

   </server>
</plugin>