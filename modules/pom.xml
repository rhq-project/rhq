<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.rhq</groupId>
    <artifactId>rhq-parent</artifactId>
    <version>1.0.0.RC11</version>
  </parent>

  <groupId>org.rhq</groupId>
  <artifactId>rhq-modules-parent</artifactId>
  <packaging>pom</packaging>

  <name>RHQ Modules</name>
  <description>parent POM for all RHQ modules</description>

  <scm>
    <connection>scm:svn:http://svn.rhq-project.org/repos/rhq/tags/RHQ_1_0_0_RC11/modules</connection>
    <developerConnection>scm:svn:http://svn.rhq-project.org/repos/rhq/tags/RHQ_1_0_0_RC11/modules</developerConnection>
  </scm>

  <properties>
    <scm.module.path>modules/</scm.module.path>
  </properties>

  <profiles>

    <profile>
      <id>default</id>
      <!-- everything except enterprise/server/container and embedded -->
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <modules>
        <module>core</module>
        <module>plugins</module>
        <module>helpers</module>
        <module>enterprise</module>
      </modules>
    </profile>

    <profile>
      <id>enterprise</id>
      <activation>
        <property>
          <name>enterprise</name>
        </property>
      </activation>
      <modules>
        <module>core</module>
        <module>plugins</module>
        <module>helpers</module>
        <module>enterprise</module>
      </modules>
    </profile>

    <profile>
      <id>agent</id>
      <activation>
        <property>
          <name>agent</name>
        </property>
      </activation>
      <modules>
        <module>core</module>
        <module>enterprise/comm</module>
        <module>enterprise/agent</module>
      </modules>
    </profile>

    <profile>
      <id>all</id>
      <activation>
        <property>
          <name>all</name>
        </property>
      </activation>
      <modules>
        <module>core</module>
        <module>plugins</module>
        <module>helpers</module>
        <module>enterprise</module>
      </modules>
    </profile>

  </profiles>

</project>